# Production Environment Configuration for Osprey Labs
# Copy this file to .env.production and update with your production values

# OpenAI Configuration
OPENAI_API_KEY=sk-your-production-openai-api-key-here

# Database Configuration (Production)
DB_HOST=postgres
DB_PORT=5432
DB_NAME=osprey_labs
DB_USER=osprey_user
DB_PASSWORD=osprey_secure_password_2024

# Application Configuration
NODE_ENV=production
NEXTAUTH_SECRET=osprey-nextauth-secret-2024-production-key
NEXTAUTH_URL=http://localhost

# Security Notes:
# 1. Generate DB_PASSWORD with: openssl rand -base64 32
# 2. Generate NEXTAUTH_SECRET with: openssl rand -base64 32
# 3. Update NEXTAUTH_URL to your actual production domain
# 4. Never commit this file to version control
# 5. Change default admin password after first deployment 